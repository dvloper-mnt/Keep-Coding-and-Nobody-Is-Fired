{
  "name": "mem_delete",
  "description": "Delete an observation by ID. Soft-delete by default; set hard_delete=true for permanent deletion.",
  "inputSchema": {
    "properties": {
      "hard_delete": {
        "description": "If true, permanently deletes the observation",
        "type": "boolean"
      },
      "id": {
        "description": "Observation ID to delete",
        "type": "number"
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  }
}